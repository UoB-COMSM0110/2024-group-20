/*class GlassButton implements Clickable {
    int x, y, width, height;

    GlassButton(int x, int y, int width, int height) {
        this.x = x;
        this.y = y;
        this.width = width;
        this.height = height;
    }

    @Override
    public boolean isMouseOver(int mouseX, int mouseY) {
        return mouseX >= x && mouseX <= x + width && mouseY >= y && mouseY <= y + height;
    }

    @Override
    public void onClick() {
        // Code to execute when the wood button is clicked. For example:
        // Set the current material to wood so that the next placement will be a wood block.
        currentMaterial = "glass";
    }
}*/
